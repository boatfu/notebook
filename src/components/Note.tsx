import React from 'react';
import {Text, View} from 'react-native';

export default class Note extends React.Component<any> {
  render() {
    return (
      <View>
        <Text>hello note</Text>
      </View>
    );
  }
}
